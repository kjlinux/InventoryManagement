#ifndef TOOLS_H
#define TOOLS_H
#include <iostream>

using namespace std;
int existe(string produit);
std::string Date();
void menu();
void retourMenuPrincipal(std::string message);
void verifierEntree(std::string valeur);
#endif